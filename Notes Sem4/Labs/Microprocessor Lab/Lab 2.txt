1  ) DATA SEGMENT 
	ARRAY1 DB 11H,22H,33H,44H,55H,66H,77H,88H,99H,10H
	ARRAY2 DB 05 DUP(0)
	COUNT EQU 10H 
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE,DS:DATA 
START :
	MOV AX,DATA
	MOV DS,AX
	LEA SI,ARRAY1
	LEA DI,ARRAY2
	MOV CH,00H
	MOV CL,COUNT
	ADD SI,0009H
NEXT:
	MOV AL,[SI]
	MOV [DI], AL
	DEC SI,
	INC DI,
	LOOP NEXT
	MOV AH,4CH
	INT 21H
CODE ENDS
END START


2 
DATA SEGMENT 
	ARRAY1 DB 11H,22H,33H,44H,55H
	ARRAY2 DB 05 DUP(0)
	COUNT EQU 05H 
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE,DS:DATA 
START :
	MOV AX,DATA
	MOV DS,AX
	LEA SI,ARRAY1
	LEA DI,ARRAY2
	MOV CH,00H
	MOV CL,COUNT
	ADD SI,0009H
NEXT:
	MOV AL,[SI]
	MOV [DI], AL
	DEC SI,
	INC DI,
	LOOP NEXT
	MOV AH,4CH
	INT 21H
CODE ENDS
END START



2A 
DATA SEGMENT
        A DW 1A10H, 2B20H, 3C30H, 4D40H, 5E50H, 2 DUP(?)
        N DW 5
        N_OVERLAP DW 3
        DATA ENDS

CODE SEGMENT
        ASSUME CS:CODE DS:DATA

START:MOV DX,DATA
      MOV DS,DX
      MOV SI,N
      DEC SI; SI=N-1
      MOV DI,SI
      ADD DI,N; DI=2*N-1
      SUB DI,N_OVERLAP
      MOV CX,N

BACK:MOV AX,[SI]
     MOV [DI],AX
     DEC SI
     DEC SI
     DEC DI
     DEC DI
     LOOP BACK

     MOV AH,4CH
     INT 21H

     CODE ENDS
     END START
        
2B
DATA SEGMENT
        A DW 1A10H, 2B20H,3C30H,4D40H,5E50H
        B DW 5 DUP(?)
DATA ENDS

CODE SEGMENT
        ASSUME CS:CODE DS:DATA

START:MOV SI,OFFSET A
      LEA DI,B
      MOV CX,05

BACK:MOV AX,[SI]
     MOV [DI],AX
     INC SI
     INC SI
     INC DI
     INC DI
     LOOP BACK

     MOV AH,4CH
     INT 21H

     CODE ENDS
     END START




qUIZ TILL LAB 8
INSEM FROM LAB 4 TO LAB 8

